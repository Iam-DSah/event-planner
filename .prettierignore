# Prettier ignores node_modules on its own; everything here is additional.

dist
docs
package-lock.json

# Markdown is excluded on purpose: its tables are hand-aligned for reading and
# Prettier reflows them into something worse.
*.md
